# site.yml at the root level of your repository
- hosts: localhost
  become: yes
  roles:
    - role: roboshop-ansible/roles/frontend
      vars:
        env: "{{ env }}"
        role_name: "{{ role_name }}"